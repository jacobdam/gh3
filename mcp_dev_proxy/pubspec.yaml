name: mcp_dev_proxy
description: A development proxy for MCP servers with crash reporting and hot-reload capabilities
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  logging: ^1.2.0
  meta: ^1.9.0
  watcher: ^1.1.0
  yaml: ^3.1.0

dev_dependencies:
  build_runner: ^2.4.0
  lints: ^3.0.0
  mockito: ^5.4.0
  test: ^1.24.0

executables:
  mcp_dev_proxy: