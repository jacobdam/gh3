name: mcp_dev_proxy
description: A development proxy for MCP servers with crash reporting and hot-reload capabilities
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  logging: ^1.2.0
  watcher: ^1.1.0
  meta: ^1.9.0

dev_dependencies:
  test: ^1.24.0
  mockito: ^5.4.0
  build_runner: ^2.4.0

executables:
  mcp_dev_proxy: