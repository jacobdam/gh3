targets:
  $default:
    builders:
      ferry_generator|graphql_builder:
        enabled: true
        options:
          schema: gh3|lib/github_schema.graphql
      ferry_generator|serializer_builder:
        enabled: true
        options:
          schema: gh3|lib/github_schema.graphql
